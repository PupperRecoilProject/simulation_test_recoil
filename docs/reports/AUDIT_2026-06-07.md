# 稽核報告 — 死碼 / config / 依賴（2026-06-07，T02-1/2/3，純讀）

> 三項稽核合一。原則：**只報告、不刪除**（刪碼另走授權任務）。將就項一併記入 REFACTOR_SCOPE。

---
## T02-1 死碼 / 未使用偵測

### 孤兒檔 / 死碼
- 🔴 `tree_demo_server.py`（根目錄）：NanoOwl 影像伺服器，`import cv2` 缺依賴即崩；唯一引用是 main 的 Popen。
  → **已授權刪除（T02-9）**。
- 🔴 `_update_recoil_warning_timer()`：無呼叫者（自動後座力斷線）→ **T02-8 接回**，非刪除。詳見 INVESTIGATION_recoil_wiring。
- 🟡 `main_nicegui.py` 的 `nanoowl_process`(全域) + `nanoowlcleanup_subprocess()`：隨 T02-9 一併移除。
- 🟡 README 仍引用 `src/simulation/observation.py`（舊觀察器），但該檔**已不存在**（git ls-files 無）→ README 過時引用，T02-6 修正。

### test/ 目錄：混入大量「非測試」工具腳本（pytest 收集會誤抓）
- 真 pytest：`test_hardware_handoff.py`(綠) / `test_serial_utils.py`。
- 一次性工具/手動腳本（非 pytest，建議移到 `tools/` 或 `scripts/`）：`dump_project.py`、`project_overview.py`
  （+`project_overview_config.yaml`）、`pyserial_console.py`、`pyserial_direct.py`、`verify_model_mode.py`、
  `test_teensy_connection.py`、`export_commit_once.ps1`、`export_commits.ps1`。
- 🔴 `test_joystick.py`：import 期呼叫 `exit()` → pytest 收集整套炸掉（D-1）→ **T02-4 處理**。
- 建議（未執行）：把工具腳本與單元測試分目錄，pytest 只掃真測試。記 REFACTOR。

---
## T02-2 config.yaml 逐鍵稽核

| 鍵 | 用途 | 備註 |
|---|---|---|
| `mujoco_model_file` | MuJoCo 場景 xml | OK |
| `onnx_models.*` | 9 個模型 + observation_recipe | OK（權威 obs 定義之一） |
| `policy_transition_duration` | 模型平滑切換秒數 | OK |
| `num_motors` / `physics_timestep` / `control_freq` | 核心 | `control_freq` 一值兼三職（C3-5） |
| `rendering_frequency` | 渲染頻率 | OK |
| `warmup_duration: 0.0` | 暖身 | 🟡 值為 0，疑似無效/未用 → 確認是否死鍵 |
| `command_scaling_factors: [1,1,1,1]` | 指令縮放 | 🟡 註解寫「順序 [vy,vx,wz]」(3 軸) 但實際 4 值 → 註解過時、與 4D command 不一致 |
| `keyboard_velocity_adjust_step` | 鍵盤步進 | OK |
| `gamepad_sensitivity.*` | 手把靈敏度 | OK；`wz:-1.5` 含方向 |
| `param_adjust_steps.*` | 調參步進 | OK |
| `default_pose` | 站立姿態(全 0) | OK，需與 xml home keyframe 一致 |
| `initial_tuning_params.*` | kp/kd/action_scale/bias | OK |
| `cascade_controller.*` | 級聯增益 | OK；註解「應參考訓練腳本」=隱性耦合 |
| `observation_recipes`(31/48) | 舊 obs 配方 | 🔴 **整段被註解掉**（死 config）→ 建議刪 |
| `floating_controller.*` | 懸浮 PID | OK |
| `terrain_generation.*` | 地形高度參數 | OK；但場域整體待重設計（REFACTOR_DECISIONS） |
| `firearm_recoil_warming.auto_inhibit: true` | 後座力自動抑制 | 🔴 預設關自動；且 switch/timer 斷線（見 recoil 報告） |
| `sim2real_motor_calibration.correction_vector` | 馬達方向校正 | OK（INTERFACE_CONTRACT 權威） |

- 將就項：滿布 `【vX.X.X】` 版本註解（與 C2-6 同）、被註解掉的 `observation_recipes`、`warmup_duration` 疑死鍵、
  `command_scaling_factors` 註解過時。記 REFACTOR C-CFG。

---
## T02-3 依賴稽核（實際 import vs README pip 清單）

README pip：`onnxruntime-directml numpy scipy PyYAML Pillow pyserial nicegui pygame`
實際第三方 import：`PIL(Pillow), numpy, onnxruntime, yaml(PyYAML), serial(pyserial), nicegui, pygame, mujoco, glfw`

| 套件 | README | 實際用 | 結論 |
|---|---|---|---|
| numpy / PyYAML / Pillow / pyserial / nicegui / pygame / onnxruntime | ✅ | ✅ | 一致 |
| **scipy** | ✅ | ❌ 全無 import | 🔴 **多餘**，README 移除（T1 已證） |
| **mujoco** | 列在「可選第2章」 | ✅ 核心(sim 必需) | 🟡 主安裝清單**漏列**，應升為核心依賴 |
| **glfw** | ❌ 未列 | ✅ keyboard/視窗用 | 🔴 **漏列**，README 補上 |
| onnxruntime-**directml** | ✅(directml 變體) | `import onnxruntime` | 🟡 env 裝的是純 onnxruntime；GPU 變體屬選配，應註明 |
| subprocess | — | 僅 NanoOwl 用 | T02-9 移除後可不依賴 |

- 版本釘選建議（環境實測值，待 Harrison 確認後寫 requirements.txt）：numpy 2.x、onnxruntime≈1.18+、
  nicegui、pygame 2.6、pyserial 3.5、mujoco 3.x、glfw 2.x、PyYAML 6.x、Pillow。
- 建議建立 `requirements.txt` / `environment.yml`（取代 README 散落 pip 行）→ 記 REFACTOR、可排 T03。
